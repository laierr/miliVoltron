# Optional deps for the standalone make_figures.py tool.
# Not required by the miliVoltron collector itself.
#
# Setup:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements-figures.txt
#
# Or with uv:
#   uv venv .venv
#   uv pip install -r requirements-figures.txt
#
# Run:
#   .venv/bin/python make_figures.py bat-log/SESSION.csv -o figures/

pandas
numpy
matplotlib
